import React, { Component } from 'react';

const mapStateToProps = state => ({
  user: state.user,
});

class Deck extends Component {

  render() {
    return (
      <div>
        
      </div>
    )
  }
}

export default Deck;
